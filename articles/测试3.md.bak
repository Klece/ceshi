# 用Python写爬虫的心得

最近写了个 B 站爬虫，记录一下过程。

## 用到的库

- `requests`：发送 HTTP 请求
- `BeautifulSoup` / `re`：解析 HTML
- `json`：处理 API 返回数据

## 遇到的坑

### 1. 请求头伪装

不加 User-Agent 会被直接拒绝：

```python
headers = {
    "User-Agent": "Mozilla/5.0 ...",
    "Referer": "https://www.bilibili.com"
}
```

### 2. API 限制

同一个 IP 请求太频繁会被封，加了个简单的延时：

```python
import time
time.sleep(random.uniform(1, 3))
```

### 3. 编码问题

有时候返回的数据是乱码，指定 `utf-8` 解码就好。

## 小结

爬虫不难，但要注意礼貌爬取，别给人家服务器造成压力。
